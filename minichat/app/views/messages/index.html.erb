<ul id="chat">
  <% if @messages %>
  	<%= render 'message', messages: @messages %>
  <% end %>
</ul>

<%= form_for Message.new, :remote => true do |f| %>
  <%= f.text_field :content %>
  <%= f.submit "Send" %>
<% end %>
