// $("#chat").append("<%= escape_javascript render(@message) %>");
// $("#new_message")[0].reset();

// $(document).ready(function() {

// 	Chat.Messenger.view = new Chat.Messenger.View(selectors);
//   Chat.Messenger.controller = new Chat.Messenger.Controller(Chat.Messenger.view, selectors);
//   new Chat.Messenger.Binder(Chat.Messenger.controller, selectors).bind();
// })